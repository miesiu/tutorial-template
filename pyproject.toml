[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Sinersio Cloud"
authors = [{name = "Sinersio", email = "support@sinersio.com"}]
dynamic = ["version", "description"]
